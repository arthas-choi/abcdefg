plugins {
    id 'java'
}

group 'com.bespin.devops'
version '1.0-SNAPSHOT'

repositories {
    mavenLocal()
    maven {
        url = uri('http://repo.maven.apache.org/maven2')
    }
}


    dependencies {
        implementation 'org.keycloak:keycloak-services:9.0.0'
        implementation 'org.keycloak:keycloak-server-spi:9.0.0'
        implementation 'org.keycloak:keycloak-server-spi-private:9.0.0'
    }

    tasks.withType(JavaCompile) {
        options.encoding = 'UTF-8'
    }
